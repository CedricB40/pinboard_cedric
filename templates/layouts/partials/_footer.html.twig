<footer class="bg-dark text-light py-4 mt-5">
    <div class="container text-center">
        <p class="mb-1">Pinboard &copy; {{ "now"|date('Y') }}</p>
        <p class="mb-0">
            <a href="{{ path('app_home') }}" class="text-light text-decoration-none">Accueil</a>
        </p>
    </div>
</footer>